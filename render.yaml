services:
  - type: web
    name: questionnaire-app
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    healthCheckPath: /api/survey/questions
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
